- name: "Apply Kubernetes manifests"
  description: "apply the Kubernetes manifests to the existing cluster"
  cmd: "echo 'Hola desde el paso deploy, aplicando los manifiestos de Kubernetes'"
  workdir: "./k8s"
  templates:
    - "service.yaml"
